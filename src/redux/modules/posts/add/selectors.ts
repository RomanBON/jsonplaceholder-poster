import { createSelector } from "reselect";

import { POST_ADD__REQUEST } from "./types";

// export const state = (state: IReduxState) => state.wall.wallAdd;
//
// export const isPending = createSelector(
//   state,
//   (state: IState<IData<IWall>>) => state.type === POST_ADD__REQUEST
// );
//
// export const wall = createSelector(state, (state: IState<IData<IWall>>) => state.payload.wall);
//
// export const type = createSelector(state, (state: IState<IData<IWall>>) => state.type);
//
// export const error = createSelector(state, (state: IState<IData<IWall>>) => state.error);
